import styled from 'styled-components';

const Link = styled.a`
    color: #09f;
    cursor: pointer;
    text-decoration: none;

    &:hover {
        text-decoration: underline;
    }
`;

export default Link;
