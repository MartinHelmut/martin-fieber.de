import styled from "styled-components";

const Text = styled.p`
  font-family: var(--base-font);
  font-size: 1.4rem;
  font-weight: 300;
  line-height: 1.65;
  margin: 0 0 var(--size-500) 0;
  padding: 0;
`;

export default Text;
